require 'json'

# Fastlane evaluates Appfile with its directory as the working directory.
app_identifier(JSON.parse(File.read('release.json')).fetch('bundleId'))
team_id(ENV['APPLE_TEAM_ID']) if ENV['APPLE_TEAM_ID']
